<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns:fx="http://javafx.com/fxml"
      fx:controller="tn.finhub.controller.AdminHomeController"
      style="-fx-padding: 30; -fx-background-color: transparent;" spacing="20">
    
    <VBox spacing="5">
        <Label text="%admin.home.overview" styleClass="header-text"/>
        <Label text="%admin.home.subtitle" styleClass="sub-header-text"/>
    </VBox>

    <!-- Stats Grid -->
    <FlowPane hgap="20" vgap="20" prefWrapLength="800">
        
        <!-- Users Card -->
        <VBox style="-fx-background-color: -color-card-bg; -fx-background-radius: 12; -fx-padding: 20; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.1), 5, 0, 0, 0);" minWidth="220">
                <Label text="%admin.home.total.users" style="-fx-text-fill: -color-text-secondary; -fx-font-size: 14px;"/>
                <Label fx:id="totalUsersLabel" text="%common.loading" style="-fx-text-fill: -color-text-primary; -fx-font-size: 24px; -fx-font-weight: bold;"/>
        </VBox>

        <!-- Volume Card -->
        <VBox style="-fx-background-color: -color-card-bg; -fx-background-radius: 12; -fx-padding: 20; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.1), 5, 0, 0, 0);" minWidth="220">
                <Label text="%admin.home.total.volume" style="-fx-text-fill: -color-text-secondary; -fx-font-size: 14px;"/>
                <Label fx:id="totalVolumeLabel" text="%common.loading" style="-fx-text-fill: -color-text-primary; -fx-font-size: 24px; -fx-font-weight: bold;"/>
        </VBox>

        <!-- Tickets Card -->
        <VBox style="-fx-background-color: -color-card-bg; -fx-background-radius: 12; -fx-padding: 20; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.1), 5, 0, 0, 0);" minWidth="220">
                <Label text="%admin.home.open.tickets" style="-fx-text-fill: -color-text-secondary; -fx-font-size: 14px;"/>
                <Label fx:id="openTicketsLabel" text="%common.loading" style="-fx-text-fill: -color-text-primary; -fx-font-size: 24px; -fx-font-weight: bold;"/>
        </VBox>

        <!-- System Health Card -->
            <VBox style="-fx-background-color: -color-card-bg; -fx-background-radius: 12; -fx-padding: 20; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.1), 5, 0, 0, 0);" minWidth="220">
                <Label text="%admin.home.system.status" style="-fx-text-fill: -color-text-secondary; -fx-font-size: 14px;"/>
                <Label fx:id="systemHealthLabel" text="%admin.home.unknown" style="-fx-text-fill: -color-text-muted; -fx-font-size: 24px; -fx-font-weight: bold;"/>
        </VBox>

    </FlowPane>
</VBox>
